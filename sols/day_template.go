package sols


import (
	// "bufio"
	// "os"
	// "fmt"
	// "strings"
	// "strconv"
)


func part1DayX() int {
	res := 0
	
	// scanner := bufio.NewScanner(os.Stdin)
	// for scanner.Scan() {
		// line := scanner.Text()
	// }

	return res
}

func part2DayX() int {
	res := 0
	
	// scanner := bufio.NewScanner(os.Stdin)
	// for scanner.Scan() {
		// line := scanner.Text()
	// }

	return res
}

func DayX(part string) int {
	if part == "part1" {
		return part1DayX()
	} else {
		return part2DayX()
	}
}
